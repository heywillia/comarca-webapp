# app.py
import streamlit as st
import pandas as pd

st.set_page_config(page_title="Comarca WebApp", layout="wide")

st.title("Comarca del Sol - Guía de Servicios")
st.write("Esta es una versión corregida del archivo app.py sin líneas con 'Ellipsis'.")

# Asegurarse de que la app se muestra
st.success("La app se cargó correctamente.")
